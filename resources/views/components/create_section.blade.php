{{--
    The old hero band (greeting, calendar widget, inline search bar, Add Exam /
    Add File buttons) was retired when the headers became permanently pinned:

    - Search      → header search icon / Ctrl+K command palette
    - Calendar    → header calendar icon button (opens the calendar modal)
    - Add Exam /
      Add File    → header "+ Create" dropdown
    - Greeting    → avatar dropdown (name, email, role)

    This file is intentionally empty (rather than deleted) so the ~35 pages that
    still `@include('components.create_section')` keep working unchanged.
--}}
